/* @jsx m */
import m from 'mithril'

// Standard button component

export const Button = () => {

    return {
        view: (vnode) => {
            return (
                <div></div>
            )
        }
    }
}