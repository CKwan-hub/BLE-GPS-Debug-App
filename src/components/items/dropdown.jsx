/* @jsx m */
import m from 'mithril'

// Standard dropdown component

export const Drowdown = () => {

    return {
        view: (vnode) => {
            return (
                <div></div>
            )
        }
    }
}