/* @jsx m */
import m from 'mithril'

// Dynamic information area

export const Info = () => {

    return {
        view: (vnode) => {
            return (
                <div></div>
            )
        }
    }
}