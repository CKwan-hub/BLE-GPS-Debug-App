/* @jsx m */
import m from 'mithril'

// Standard input component

export const Input = () => {

    return {
        view: (vnode) => {
            return (
                <div></div>
            )
        }
    }
}