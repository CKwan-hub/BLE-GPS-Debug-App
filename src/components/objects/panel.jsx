/* @jsx m */
import m from 'mithril'

// "Sidebar" style panel for housing buttons and other components

export const Panel = () => {

    return {
        view: (vnode) => {
            return (
                <div></div>
            )
        }
    }
}